machine:
  node:
    version: 6.1.0
  services:
    - docker
    - rabbitmq-server

dependencies:
  post:
    - docker build -t $AWS_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/topcoder-connect:$CIRCLE_SHA1 .

deployment:
  prod:
    branch: master
    commands:
      - ./deploy.sh
